/**
 * Окружение для тестов SFU.
 *
 * `src/config.ts` валидируется НА ИМПОРТЕ и намеренно падает, если при
 * wildcard-listenIp не задан announcedIp (без этого клиенты получают SDP с
 * 0.0.0.0 и чёрное видео — лучше не стартовать вовсе). Любой тест, который
 * хоть по цепочке импортов доходит до конфига, обязан сначала выставить эти
 * переменные. `setupFiles` выполняется до импорта файла теста, поэтому
 * место верное.
 */
process.env.WEBRTC_ANNOUNCED_IP ??= '127.0.0.1';
process.env.SIGNALING_REQUIRE_AUTH ??= 'false';
process.env.JWT_SECRET ??= 'test-secret';
// Тесты проверяют поведение кода, а не политику подмен: strict-режим
// превратил бы любой fallback в исключение и спрятал бы проверяемое.
process.env.HTQ_ENV ??= 'production';
