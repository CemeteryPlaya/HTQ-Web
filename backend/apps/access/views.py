"""HTTP-слой ``/api/access/v1/*`` — class-based вьюхи.

Стиль тот же, что в ``apps.contracts`` и ``apps.signoff``: ``htqweb.http.ApiView``,
``api_view`` навешивается пометодно через ``method_decorator``. Пометодно — не
из вкусовых соображений: режим авторизации у методов одного URL разный (``GET``
каталога доступен любому вошедшему, мутации каталога — только платформенному
администратору, спека §4.1), а ``api_view`` связывает ровно один режим с одной
функцией.

Наполняется в задаче 7 плана; здесь — точка монтирования маршрутов.
"""

from htqweb.http import ApiView


class RoleCollectionView(ApiView):
    pass
