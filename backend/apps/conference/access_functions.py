"""Функции видеоконференций для реестра прав.

Модуль — как раз тот случай, ради которого узлы объявляют ПРИМЕНИМЫЕ признаки.
Вход в конференцию — действие, а не запись: «видит» у него означает «может
войти», а «удалять» не означает ничего. Пока признаки были одинаковыми для
всех узлов, роль «видеоконференция: только удалять» заводилась беспрепятственно
и никто не мог сказать, пустят ли по ней в комнату.

Разделение «обычный участник / администратор» проходит здесь по функциям, а не
по глубине: рядовому нужен ``conference.join``, администратору — история,
записи и приглашения.

⚠️ Право войти в КОНКРЕТНУЮ комнату этой моделью не описывается и описываться
не должно: его выдаёт SFU по платформенному JWT и гостевому токену с claim
``room_id`` (``sfu/src/auth.ts``). Здесь — только «пускать ли этого человека в
конференции вообще».
"""

VIEW = ("view",)

FUNCTIONS = (
    ("conference.join", "Участие в конференциях", VIEW),
    ("conference.history", "История встреч", ("view", "delete")),
    ("conference.recordings", "Записи встреч", ("view", "delete")),
    ("conference.transcripts", "Протоколы встреч", ("view", "delete")),
    ("conference.invites", "Приглашения по ссылке", ("view", "create", "delete")),
)

MODULE_KIND = "functional"
