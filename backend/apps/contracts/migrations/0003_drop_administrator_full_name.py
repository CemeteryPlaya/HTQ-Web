"""Снятие ФИО с администратора бюджета.

Заказчик убрал имя человека из записи: администратор бюджета опознаётся
проектом и страной, а кто им занимается — меняется чаще, чем сам проект.
Подпись записи становится «проект страна» (``Administrator.__str__``),
сортировка справочника — по проекту.

Миграция НЕОБРАТИМА по данным: ``full_name`` удаляется вместе со
значениями, откат вернёт колонку пустой. Записей мало и они справочные,
поэтому переносить их некуда — оставлять ФИО «на всякий случай» в
неотображаемой колонке хуже: она разъедется с реальностью и однажды
всплывёт в выгрузке.

Уникальность на пару (проект, страна) НЕ добавляется, хотя после снятия
ФИО это вся идентичность записи. Существующие строки могли различаться
только именем, и такой констрейнт уронил бы миграцию на живой базе.
Схлопывание дублей — отдельный шаг, когда данные будут проверены;
``budget_service._resolve_administrator`` до тех пор ищет по той же паре
через ``get_or_create``.
"""

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('contracts', '0002_approval_state'),
    ]

    operations = [
        migrations.AlterModelOptions(
            name='administrator',
            options={'ordering': ('project_name',), 'verbose_name': 'Администратор бюджета', 'verbose_name_plural': 'Администраторы бюджета'},
        ),
        migrations.RemoveField(
            model_name='administrator',
            name='full_name',
        ),
    ]
