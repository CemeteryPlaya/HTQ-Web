# Тест-файлы vitest, исключённые из прогона в CI.
#
# Мотив — тот же, что у backend/ci-known-failures.txt: CI должен быть зелёным,
# иначе его перестанут читать и новая поломка потеряется среди старых.
# Строка здесь — ДОЛГ с TODO, а не исключение. Починили — удалите строку.
#
# ⚠️ Исключение файловое, а не потестовое: vitest фильтрует по путям. Значит,
# вместе с восемью падающими из прогона уходят и соседние проходящие тесты
# этих же двух файлов — цена принята осознанно, потому что оба файла целиком
# про одну и ту же сломанную фичу.
#
# ── Карточка сотрудника, секции Т-2 ──────────────────────────────────────────
# TODO: фича сломана СКВОЗНО, и это главное, что стоит знать. Те же самые
# «секции Т-2» роняют и два backend-теста:
#   apps/hr/tests/test_employees_api.py::test_create_employee_with_card_t2_writes_card
#   apps/hr/tests/test_employees_api.py::test_update_employee_with_card_t2_applies_both
# То есть это ОДИН дефект (карточка не создаётся/не обновляется), а не десять
# разных, и чинить его надо с бэкенда: если POST не пишет EmployeeCard, фронт
# зелёным не станет никакими правками.
# Проверено 2026-09-02: падает 8 из 496 тестов, оба файла — про Т-2.
src/components/hr/__tests__/CardT2SectionDialog.test.tsx
src/components/hr/__tests__/EmployeeFormDialog.test.tsx

# ── Не тесты, но тот же долг ─────────────────────────────────────────────────
# Строк ниже нет и быть не должно (файл читает только список --exclude), это
# запись для человека.
#
# TODO: ТИПЫ В CI НЕ ПРОВЕРЯЮТСЯ ВООБЩЕ. Шаг «Типы» выполняет
#   `npx tsc --noEmit -p tsconfig.json`, а корневой tsconfig.json — solution-файл
#   с `"files": []` и только `references`. Такой вызов берёт НОЛЬ файлов:
#   `npx tsc --noEmit -p tsconfig.json --listFiles` печатает пустоту. То же
#   сказано в шапке eslint.config.js — там из-за этого и включили
#   react/jsx-no-undef, единственную сеть, которая ловит неопределённые
#   идентификаторы в JSX.
#
#   Прежняя запись здесь утверждала, что команда «покрывает код приложения», а
#   `tsc -b` падает лишь на двух ошибках в vite.config.ts. Проверено 2026-09-02:
#   `npx tsc -b` даёт 316 ошибок, из них 300 — в коде приложения
#   (`tsconfig.app.json`), остальные в конфигах сборки. Настоящий масштаб долга
#   виден командой:
#       npx tsc --noEmit -p tsconfig.app.json
#
#   Пока долг не разобран, правки проверяют так: прогнать команду выше и
#   убедиться, что в СВОИХ файлах ошибок нет (сравнив вывод до и после).
#   Переключать шаг CI на tsconfig.app.json бессмысленно, пока этих 300 ошибок
#   не станет ноль: он сразу станет вечно красным.
#
# TODO: линт-долг — frontend/eslint.ci.config.js, там свой список с TODO.
