# Мультикомпанейность — что осталось незакрытым

**Дата:** 2026-08-28
**Ветка:** `structure-refactoring`
**Спека:** [multi-company-tenancy-design.md](multi-company-tenancy-design.md)
**План (выполнен):** [../docs/plans/2026-08-27-multi-company-tenancy-core.md](plans/2026-08-27-multi-company-tenancy-core.md)

**Состояние на 24.09.2026:** закрыты пункты 1, 2, 3 (блоком K), 5, 6, 7; пункт 4 — частично (архив «только чтение» не сделан); итог рефакторинга структуры группы — [roadmap §10](plans/2026-09-14-group-structure-roadmap.md#10-итог-рефакторинга-23092026).

Подпроект 1 «Ядро тенантности» выполнен: 18 задач, 4545 тестов проходят, регрессий
относительно состояния до работ нет. Итоговое ревью ветки нашло три Critical — все
три оказались **дырами между задачами**, которых не видела ни одна задачная
проверка. Две из них закрыты; остальное собрано здесь.

Документ существует потому, что часть найденного требует **решений**, а не правок,
и потому что часть пунктов делает платформу неработоспособной после переноса
боевых данных. Читать до запуска `tenancy_bootstrap` на бою.

## Блокирует запуск на бою

### 1. Задачи Celery не переведены на контекст компании — ЗАКРЫТО

Коммиты `98f913f`, `f97ba73`, `592d992`, `55649ae` (28.08.2026).
Все три носителя переведены на шаблон «диспетчер + веер»: `sync_identity`,
`task_deadline_reminder`, `calendar_event_reminder` помечены `@company_task`,
рядом заведены диспетчеры (`sync_identity_dispatch`,
`task_deadline_reminder_dispatch`, `calendar_event_reminder_dispatch`), beat
переключён на них миграциями `hr/0020_sync_identity_dispatch_periodic_task` и
`tasks/0019_tasks_periodic_tasks_use_dispatchers` (обе — в
`SHARED_EFFECT_MIGRATIONS`). Мета-тест
`apps/core/tests/test_invariants.py::test_tenant_app_tasks_use_company_task_or_are_marked_dispatchers`
падает в CI на любой tenant-задаче без `@company_task`/`@company_dispatch_task`.
Ниже — исходная постановка, оставлена для истории.

`grep` по `htqweb.tenancy` в `apps/{hr,tasks,contracts,signoff}` даёт ноль
совпадений. После `tenancy_bootstrap` тенантных таблиц в `public` нет, а у задач
Celery нет HTTP-запроса, значит контекст компании пуст и `search_path = public`.

Ломается:

| Задача | Расписание | Последствие |
|---|---|---|
| `apps/hr/tasks.py::sync_identity` | ночью | `ProgrammingError` на `hr_employee` |
| `apps/tasks/tasks.py::task_deadline_reminder` | ежечасно | напоминания о сроках прекращаются для всей платформы |
| `apps/tasks/tasks.py::calendar_event_reminder` | каждые 5 минут | напоминания о встречах прекращаются |

При этом механизм построен (`htqweb/tenancy/celery.py::company_task`), а
`backend/README.md` правило 12 уже требует его применять — но ни один носитель на
него не переведён.

**Шаблон решения — «диспетчер + веер»:** beat планирует диспетчерскую задачу без
компании; она читает `active_company_slugs()` и ставит по одной реальной задаче на
компанию, передавая `company_slug` **именованным** аргументом; реальная задача
помечена `@company_task`. Сбой на одной компании не отменяет остальные.

Плюс расширить мета-тест в `apps/core/tests/` — сегодня он проверяет только
`require_service` первой строкой. Задача tenant-аппки без `@company_task` обязана
падать в CI, а не на бою.

### 2. Обновление токена игнорирует компанию запроса — ЗАКРЫТО

Коммиты `cbe73b0` (28.08.2026, обмен refresh) и `c9b0306` (29.08.2026, вход
по паролю).
`issue_token_pair` принимает необязательный `company_slug`; `refresh_token` берёт
`request.company`, проверяет членство через новый предикат
`apps.companies.interface.user_may_enter_company(user_id, slug)` и **отказывает
403 `{"detail": "Forbidden"}`**, если пользователь в компании не состоит, — вместо
молчаливого отката на компанию по умолчанию. Заголовка нет — поведение прежнее
(компания по умолчанию).

Ту же дыру нашли и во второй двери: `obtain_token` (вход по паролю) звал
`issue_token_pair(user)` без компании, и пользователь на поддомене, отличном от
его компании по умолчанию, не мог войти вообще — 403 на первый же запрос, без
спасительного обмена по refresh (тот срабатывает только на 401). Общая для обеих
вьюх логика вынесена в `apps.users.views._company_slug_for_token(request,
user_id)`. Ниже — исходная постановка первой находки, оставлена для истории.

`apps/users/views.py::refresh_token` зовёт `issue_token_pair(user)`, который берёт
`default_company_slug(user.id)`. Заголовок `X-HTQ-Company` до вьюхи доходит (nginx
ставит его и на `/token/refresh`), но на выдаваемый claim не влияет.

Последствие: пользователь с членством в `kz` (по умолчанию) и `uz` переходит на
`uz.` — фронт меняет refresh-cookie на access и получает токен с `company: "kz"`,
затем 403 от `htqweb/http.py`. Одна повторная попытка даёт то же. **Пользователь
заперт в компании по умолчанию.**

Это ровно тот сценарий, ради которого §9 спеки вводила cookie на родительском
домене: механизм переноса cookie сделан верно, второй конец обмена — нет.

Решение: при выпуске токена учитывать компанию запроса, **если пользователь в ней
состоит**. Проверка членства обязательна — без неё это дыра.

## Требует проектного решения

### 3. Бизнес-метрики tenant-аппок после переноса данных — ЗАКРЫТО блоком K

**Состояние на 24.09.2026 — закрыто** (блок K, коммиты задач 1–2): веер по
компаниям с меткой `company`, решение заказчика 24.09.2026 — метка, а не
сумма по группе.

**Состояние на 23.09.2026 — не решено, и отложено туда, где его уже не
решат.** Подпроект 3 закрылся без этого вопроса: `CompanyModule` пришёл с
реестром компаний (`d6246ff`, 27.08.2026 «feat(companies): реестр компаний
группы»), а форму метрик не брали ни он, ни блоки A–I.2. Пропуск на месте —
`backend/apps/core/metrics.py:103-110`, и других
путей сбора нет: `collect_all()` берёт модули только из `_metric_modules()`
(`backend/apps/core/metrics.py:156`). С `309bdbe` (08.09.2026 «метрики всех
доменов, дашборды, 40 правил и два чата») `metrics.py` есть у всех четырёх
tenant-аппок — `hr`, `tasks`, `contracts`, `signoff`, — и все четыре
сборщиком пропускаются, хотя докстринг `apps/core/metrics.py:30` по-прежнему
называет только `apps.tasks`. **Последствие** — не просто пустая панель:
панели по 16 метрикам пусты, шесть правил алертинга с `noDataState: OK`
сработать не могут (сверка §6.4 п. 1). Пункт ждёт хозяина.

Коммит `985d778` (29.08.2026).
Развилка (агрегировать по группе или лейблировать по компании) осталась
нерешённой — **форма решается в подпроекте 3**, вместе с per-company гейтами
модулей (`CompanyModule`), потому что оба вопроса упираются в один и тот же веер
по компаниям. До тех пор `apps/core/metrics.py::_metric_modules()` пропускает
`settings.TENANT_APPS` явно (`logger.info`, не `fallback`) — метрика домена не
исчезает молча с дашборда, а просто не считается, и это видно по одной спокойной
строке в логе вместо часовой стены `FALLBACK` в Loki.

Ниже — исходная постановка, оставлена для истории.

`apps/tasks/metrics.py::collect()` вызывается из `apps/core/metrics.py` в
Celery-задаче без контекста компании. После переноса данных упадёт, сборщик поймает
падение через `fallback`, в проде режим `log` — и метрики домена **тихо исчезнут с
дашборда навсегда**, оставив только строку `FALLBACK` в Loki раз в минуту.

Развилка: агрегировать по группе или лейблировать по компании. Второе информативнее,
но меняет форму метрик и дашборды. Правкой не закрывается — нужно решение.

### 4. Архивная компания: 404 против «только чтение» — ЗАКРЫТО

**Закрыто 25.09.2026** — [спека «Архив компании — только чтение»](plans/2026-09-25-archive-read-only-spec.md),
правило в `backend/htqweb/tenancy/archive.py`. Архив больше не 404 целиком:
любой метод кроме `GET`/`HEAD`/`OPTIONS` на поддомене архива — 403
`company_archived` всем, включая суперпользователя (кроме выдачи и обновления
токена); чтение — только суперпользователю, остальным 404, в том числе по
access-токену, выданному до архивации; анонимные ручки закрыты только у
тенантных аппок, а подписанные файлы, вложения и аватары общих аппок
отвечают. Токен архива выдаётся только суперпользователю, `migrate_companies`
доводит архивные схемы (со схемой), `@company_task` архив пропускает. Остаток
подпроекта 4 — банкротство с преемником — отдельной спекой. Текст ниже —
история до закрытия.

**Состояние на 23.09.2026 — по существу без изменений.** Архив по-прежнему
404 на весь трафик компании (`backend/htqweb/middleware/company_context.py:60-63`),
гейта «чтение разрешено, запись отклоняется» нет, подпроект 4 выполнен только
частично (`multi-company-tenancy-design.md` §4). Поверх CLI блок A дал сервис жизненного цикла с гейтом
«единственную действующую компанию не архивировать» (`ec75959`, 14.09.2026,
задача 3 плана блока A) и HTTP-ручки архива и восстановления (`44a00ad`,
14.09.2026, задача 7).

Коммиты `9ae51d6` (29.08.2026, архивация пересобирает сводки) и `77533bf`
(29.08.2026, правка спеки §6/§12).
Решено и зафиксировано в спеке (§6, §12 `multi-company-tenancy-design.md`): **404
сейчас**, «только чтение» — явно и осознанно в подпроект 4, вместе с остальным
жизненным циклом (см. правку §6 — там же указан шов, куда встанет гейт записи).
Пересборка сводных представлений при архивации закрыта: `manage.py
company_archive --company <slug>` / `company_restore` переводят статус и зовут
`rebuild_holding_views()` одной операцией, `status` в `CompanyAdmin` стал
read-only (правка через админку больше не существует как отдельный, не
пересобирающий сводки путь). Что НЕ закрыто и остаётся подпроекту 4: сам гейт
«чтение разрешено, запись отклоняется» для архивной компании — сегодня архив
означает 404 на весь трафик без исключений, и это осознанный выбор, а не забытая
работа.

Ниже — исходная постановка, оставлена для истории.

Спека противоречит сама себе, и код следует одной из версий:

- §6: «архивный slug → 404 до всякой аутентификации» — так и сделано
  (`company_context.py`);
- требование заказчика: архив это **только чтение**, данные остаются доступны.

Реализовано «недоступна вовсе». Кроме того, пересборка сводных представлений при
архивации не вызывается (кода архивации нет вовсе, `status` правится через админку),
поэтому строки архивированной компании **остаются в сводках холдинга** до
следующего `migrate_companies`.

Относится к подпроекту 4 (жизненный цикл), но решить надо раньше: подпроект 4 будет
строиться на этой предпосылке.

## Известные ограничения, названные явно

### 5. Пост-bootstrap рантайм не исполнялся нигде — ЗАКРЫТО

Коммит `ab1dc82` (29.08.2026).
`frontend/vite.config.ts` теперь ставит `X-HTQ-Company` из `Host` на каждом
проксируемом к Django запросе (`companyFromHost` из
`src/lib/auth/companySwitch.ts` — та же регулярка, что и у nginx, без
четвёртой копии). nginx в тестовые compose-файлы сознательно НЕ добавлен —
решение принято: дешевле держать правило в одном месте (прокси Vite), чем
заводить четвёртое место с логикой определения компании. Проверено
вручную: запрос с `Host: kz.localhost` доходит до
`CompanyContextMiddleware` и резолвит `company=kz` (журнал ручной проверки
в git не хранится). С блока I.2 в заголовке едет метка хоста — псевдоним
или слаг, — и middleware резолвит её через `resolve_host_label`
(`backend/htqweb/middleware/company_context.py:59`, `46df849`, 23.09.2026).

Ниже — исходная постановка, оставлена для истории.

Все 4545 тестов идут на раскладке **до** переноса данных: тенантные таблицы в
`public`. Ни один разработчик и ни один тестовый стек не может пройти путь
«поддомен → заголовок → схема» целиком, потому что прокси Vite не ставит
`X-HTQ-Company`, а сервиса `nginx` в тестовых compose-файлах нет вовсе — он только
в `docker-compose.yml` под профилем `production`.

Следствие: поведение платформы после `tenancy_bootstrap` выведено по коду, а не
наблюдением. Перед боевым запуском стоит собрать стенд, где этот путь проходится.

### 6. `companySwitch` на фронте не подключён — ЗАКРЫТО блоками A и I.2

`switchCompany` из `frontend/src/lib/auth/companySwitch.ts` зовут двое:

- `CompanySwitcher` (рендер в `frontend/src/components/Header.tsx:264`, `:298`;
  вызов `switchCompany` — `frontend/src/components/companies/CompanySwitcher.tsx:33`)
  — блок A, задача 10 ([план](plans/2026-09-14-block-a-company-registry.md);
  `86549ca`, 14.09.2026 «переключатель компании в шапке»). Правило
  видимости — режим перехода: компаний больше одной либо хост уже на
  поддомене компании.
- Экран выбора компании `CompanyPicker` (`/companies/choose`,
  `frontend/src/pages/CompanyPicker.tsx`) — блок I.2, задача 6
  ([план](plans/2026-09-22-block-i2.md); `d7cc633`, 23.09.2026 «выбор
  компании на голом домене, переход по псевдониму»). С одной компанией
  уводит в неё сам, с несколькими — по выбору, и возвращает на исходный
  путь. На него `RequireAuth` отправляет с любого защищённого маршрута
  голого домена (`frontend/src/components/RequireAuth.tsx:107`).

Адрес строится `hostLabelOf` — псевдоним компании, если он задан, иначе
слаг (`d7cc633`); по слагу компании с псевдонимом Django отвечает 404
(`apps.companies.interface.resolve_host_label`, `46df849`, 23.09.2026).
`companyFromHost` стоит в живых путях: по нему прокси Vite ставит
`X-HTQ-Company` (`frontend/vite.config.ts:56`, п. 5), а `RequireAuth` и
`CompanySwitcher` определяют текущую компанию. На новом поддомене сессия
поднимается обменом refresh-cookie без второго входа — `restoreSessionOnce`
(`frontend/src/lib/auth/sessionRestore.ts`; `87d66a0`, 23.09.2026,
финальная волна блока I.2).

Было: `companyFromHost`/`switchCompany` никто не вызывал за пределами тестов,
переключателя в интерфейсе не было, и регулярка, согласованная с nginx, не
участвовала ни в одном пути выполнения.

### 7. Схема `holding` собирается, но не читается — ЗАКРЫТО блоком H

Читатели — `managed=False`-модели `backend/apps/hr/holding_models.py`
(`e475074`, 17.09.2026 «сводка по людям и штату для всей группы») и
`backend/apps/tasks/holding_models.py` (`ec31c79`, 17.09.2026 «сводка по
работам для всей группы») — задачи 2 и 4 [плана блока
H](plans/2026-09-17-block-h-holding-readers.md). Читают только внутри
`use_holding()` (`backend/apps/hr/services/holding_service.py:83`,
`backend/apps/tasks/services/holding_service.py:97`); вне его менеджер
читателя поднимает `HoldingContextRequired`. Наружу — две ручки:
`GET /api/hr/v1/holding/headcount` (`backend/apps/hr/urls.py:425`) и
`GET /api/tasks/v1/holding/projects` (`backend/apps/tasks/urls.py:267`); их
собирает экран «Сводка группы» `/holding`
(`frontend/src/pages/holding/GroupSummary.tsx`; `43c8816`, 17.09.2026,
задача 5). Читатели `holding.contracts_*` — у второго разработчика
([roadmap §6](plans/2026-09-14-group-structure-roadmap.md#6-передача-другому-разработчику-contracts--signoff), п. 6.3).

Было: `use_holding()` не вызывался нигде, кроме тестов, `managed=False`-моделей
не было — цена (представления блокируют contract-миграции, пересборка на
каждом `migrate_companies`) платилась без выгоды.

## Рантбук первого запуска на бою

Порядок, выведенный из кода. Шаги 4-5 сегодня **не работают** без закрытия
пунктов 1-2 выше.

**Состояние на 23.09.2026.** Пункты 1–2 закрыты, шаги 4–5 ими больше не
заблокированы. Порядок выкатки блоков A–I.2 —
[roadmap §7](plans/2026-09-14-group-structure-roadmap.md#7-порядок-выкатки),
поддомены и TLS — [чеклист поддоменов](deploy/subdomains-runbook.md): TLS
выбран через Cloudflare (Origin Certificate), а не DNS-01 из шага 5.

1. Выставить `RUN_MIGRATIONS=0` **явно**. `docker-compose.yml` даёт дефолт `1`
   (`${RUN_MIGRATIONS:-1}`), и ни один `.env` в репозитории его не переопределяет.
2. `manage.py tenancy_bootstrap --slug <slug> --name "..." --kind holding --dry-run`
   — проверяет наличие таблиц в `public`, свободу схемы и валидность слага, ничего
   не меняет.
3. Боевой прогон той же команды без `--dry-run`. Одна транзакция, `ALTER TABLE ...
   SET SCHEMA` — правка каталога, данные не копируются; простой равен длительности
   блокировки `ACCESS EXCLUSIVE`, то есть секундам. `--grant-all` включён по
   умолчанию и заводит членство всем активным пользователям.
4. Перезапуск контейнеров. `docker-entrypoint.sh` зовёт `migrate_shared`, тенантные
   таблицы в `public` не воскресают.
5. Перевод пользователей на поддомены. **Требует HTTPS** — refresh-cookie объявлена
   `Secure`. `infra/nginx/tls.conf.example` теперь несёт маршрутизацию по компании,
   но wildcard-сертификат нужен через **DNS-01**: HTTP-01, на который настроен
   certbot, wildcard не выдаёт.

### Аварийная ситуация, которую надо знать

Если `company.delete()` в откате `company_create` упадёт после успешного
`drop_schema`, останется активная строка реестра **без физической схемы**. После
этого `rebuild_holding_views` падает на `CREATE VIEW ... FROM co_x.hr_employee`, и
сводки холдинга не восстановить, пока строку не удалить руками.

Лечение: удалить строку `Company`, повторить `manage.py migrate_companies`.

## Двадцать отложенных мелких находок

Все двадцать разобраны итоговым ревью и признаны не блокирующими слияние. Две из
них взяты в волну правок (`--plan` лгал про обратное направление, `--dry-run`
ничего не проверял). Остальные — в истории ревью; ни одна не меняет поведения,
которое кто-то мог бы принять за правильное.

**Состояние на 23.09.2026 — поштучно не сверить.** Перечня восемнадцати
оставшихся находок в репозитории нет: он жил в журнале исполнения
подпроекта 1, который в git не хранится. Две взятые в волну правок на месте:
`--plan` проверяет обратное направление и у shared-effect шагов (`9b8261c`,
28.08.2026, правка 4а; `backend/apps/companies/services/migration_service.py:396-397`,
тест `test_plan_refuses_backwards_shared_effect_migration`),
`tenancy_bootstrap --dry-run` сверяет базу (`507d177`, 28.08.2026, правка 4б).

## Побочная находка: два теста падают ночью (дефект чужой, не наш)

Финальный прогон в 01:50 по Алматы дал 12 падений вместо десяти. Два новых:
`apps/tasks/tests/test_daily_reports_api.py::test_board_defaults_to_today` и
`apps/tasks/tests/test_project_staff_reports_api.py::test_board_defaults_to_today_and_rejects_a_malformed_date`.

**Состояние на 24.09.2026 — закрыто блоком K (задача 3, `675ddaf`):**
«сегодня» в `hr`/`tasks`/`approvals` одно — `timezone.localdate()` (пояс
`TIME_ZONE`, в проде UTC; конференции считают сутки в `PLATFORM_TIME_ZONE` —
`apps/conference/services/platform_time.py`, свести их — отдельное решение,
roadmap §9.2), сторож `apps/core/tests/test_platform_today.py`; оба теста досок закреплены на
границе суток при `UTC` и `Asia/Almaty`.

Причина установлена и к мультикомпанейности отношения не имеет:

- вьюха (`apps/tasks/views.py:884` на 29.08, сегодня `:892`) берёт
  `timezone.localdate()`, а
  `TIME_ZONE = "UTC"` — то есть дату по UTC;
- тест создаёт отчёт с `dt.date.today()` — это системная дата, Алматы (UTC+5).

С 00:00 до 05:00 по Алматы системная дата уже следующая, а UTC-дата ещё
предыдущая: тест кладёт отчёт на сегодня, доска ищет за вчера, выборка пуста.
Тесты падают каждую ночь в этом окне, независимо от изменений.

Файлы `apps/tasks/views.py` и оба тестовых файла ветка `structure-refactoring`
не трогала (`git log aabafe3..HEAD --` по ним пуст).

Лечение — привести тест к тому же источнику даты, что и вьюха
(`timezone.localdate()` вместо `date.today()`). Не делали: дефект вне объёма
работы, и трогать чужие тесты в этой ветке значило бы смешать её содержание.
