В данном документе были вопрос от Claude Opus 5.5 (1M), на все ответил под вопросом с префиксом "Ответ:", далее документ:
 Открытые вопросы по встраиванию ТЗ БЗО в платформу

- Дата: 2026-09-25 · Ветка: `new-module-BPP `

Вопросы возникли при соотнесении ТЗ с кодом. Они не повторяют вопросы, на которые ТЗ уже отвечает, — кроме случаев, когда ответ ТЗ расходится с кодом или данными.

Группы:
- A — организационные (решают Санжар и Руслан, при необходимости тимлид);
- B — к заказчику (бизнес), в начале — 23 вопроса самого ТЗ с пометкой, что по ним уже говорит платформа;
- C — архитектурные (решаем вдвоём);
- D — по существующему коду и данным (к авторам `apps.contracts`).

Поля: «Зачем» — что от ответа зависит; «По умолчанию» — вариант, по которому пойдём, если ответа не будет; «Блокер» — нельзя начинать соответствующий этап без ответа.

 Топ-10 блокирующих вопросов

|  | Вопрос | Кому | Блокирует |

| 1 | [Q-C01](1) Где живёт модуль: развиваем `apps.contracts` или новая аппка | A+B | всё | Ответ: развиваем apps.contracts
| 2 | [Q-B01](1) Единица бюджета: проект навсегда (ТЗ) или проект × страна × год × валюта (данные) | заказчик | бюджет, заявка, расчёты | Ответ: Проект × страна × год × валюта (данные)
| 3 | [Q-C04](4) Что такое «Проект»: `tasks.Project`, `contracts.Administrator` или новая сущность | A+B | бюджет, заявка, права ПМ | Ответ: Новая сущность у которой также будут подтягивать данные и tasks и Administrator для contracts
| 4 | [Q-B03](3) Коды статей свои у каждого проекта — справочник статей глобальный или по проекту | заказчик | бюджет, группы статей, BR-010 | Ответ: Общий справочник
| 5 | [Q-B06](6) Что «задействует» бюджет: формула ТЗ (от позиций заявок) или текущая (от договоров и счетов) | заказчик | все остатки, BR-011, CALC-002 | Ответ: Формула из ТЗ
| 6 | [Q-B09](9) / [Q-B10](0) Единый счёт с номером `СЧ-` и АВР/накладная как файлы или как документы | заказчик | счёт, сверка, миграция данных | Ответ: Счет на оплату, АВР, Накладная, Счет-фактуры это документы, которые будут храниться в виде файлов (pdf, xlsx, docx, xml), конкретнее понятие СЧ- и счета уточнить у Claude
| 7 | [Q-A04](4) Судьба текущего процесса «Заявка на закуп» (закупщик ×2 → CFO) | A+B, заказчик | заявка, план закупок | Ответ: Интегрировать в модуль который прописан в ТЗ
| 8 | [Q-A05](5) Живые данные contracts: мигрировать или начинать БЗО с чистого листа | A+B, заказчик | миграции, сроки | Ответ: Мигрировать
| 9 | [Q-C14](4) + [Q-B15](5) Роли ТЗ в `apps.access` и кто конкретно ФД/ТД/ОД/ГД/БУХ в каждой компании | A+B, заказчик | права, маршруты | Ответ: данные должности уже были при структурном рефакторинге и их нужно будет соотнести с уже имеющимися, и также в ТЗ было прописан транскрипт этих должностей. При этом, все эти должности относятся к компании которая находится в руководящей компании
| 10 | [Q-C09](9) Какие доработки signoff включаются для всех типов, в том числе для кадровых согласований `hr.` | A+B | signoff, маршруты | Ответ: Необходимые

 A. Организационные

Q-A01. Граница модуля по коду. Модуль БЗО — это только `contracts` + `signoff` + шаблон закупа в `approvals`, или в него входят и доработки `tasks.Project`, `apps.access`, `hr` (должности ФД/ТД/…), `htqweb` (идемпотентность, версии), уведомления?
- Зачем: определяет, что попадает в план и в оценку.
- По умолчанию: входит всё из §6.2 анализа; платформенные доработки делаются общими, а не «под БЗО».
- Блокер: нет.
Ответ: contracts + signoff+approvals+зависимости с других аппок (включая будущий модуль Закупок, который будет разрабатываться вне этого ТЗ

Q-A02. Распределение работ A/B и владение. Принимаем распределение из §6.2 анализа? Кто утверждает изменения общего движка signoff — они затрагивают согласование заявок, договоров и десяти кадровых предметов `hr.`?
- Зачем: без владельца signoff два исполнителя будут менять движок с разных сторон.
- По умолчанию: B — владелец signoff; A делает ревью изменений движка; распределение — как в §6.2, с пересмотром после оценки задач.
- Блокер: да, для плана.
Ответ: Оставить так, исполнитель В будет отправлять вопросы исполнителю А для подтверждения изменений

Q-A03. Участие Zhanibek. Основной автор `apps.contracts` (48 коммитов, модели оплат, АВР, подотчёта, импорта CashFlow). Он участвует в БЗО? Если нет — кто передаёт знания о данных и незавершённых ветках?
- Зачем: большая часть неочевидных решений — в его коде и данных заказчика.
- По умолчанию: одна встреча-передача знаний; дальнейшие правки его кода — у A/B.
- Блокер: нет.
Ответ: не учавстует, его тимлид который передавал знания, передал их теперь нам

Q-A04. Судьба текущего процесса «Заявка на закуп». Сейчас это шаблон конструктора форм (`seed_purchase_request_template`): строка бюджета, позиции без цены, маршрут «Подбор поставщика (сравнительная таблица) → Счёт на оплату (PDF, сумма = выбранному предложению) → финансовый директор», договор или счёт заводятся по одобренной заявке. ТЗ описывает другой процесс: заявка с ориентировочными ценами → ТД → ОД → План закупок → договор или счёт. Что делаем:
- (а) заменяем шаблон типизированной заявкой ТЗ и снимаем шаблон с публикации;
- (б) оставляем оба — шаблон для «прочих» закупок;
- (в) адаптируем шаблон под ТЗ, не заводя модель.
Ответ: “В” вариант будет лучшим

Что с уже поданными по шаблону заявками и договорами/счетами, которые ссылаются на них через `request_id`?
- Зачем: определяет модель заявки, План закупок и миграцию.
- По умолчанию: (а); существующие заявки дорабатываются в старом процессе до закрытия, новые создаются только по ТЗ.
- Блокер: да.
Ответ: Заявки были тестовыми и их можно снести, полного запуска проекта все ещё не было, но надо оставить справочные данные

Q-A05. Живые данные. В каких схемах компаний уже есть данные contracts (импорт CashFlow, ручной ввод) и сколько их? Нужна ли их полная миграция в новую модель (бюджет на проект, единый счёт, закрывающие документы) или БЗО стартует с чистого листа с даты X, а старое остаётся архивом?
- Зачем: от ответа зависит объём работ на 30–50% (миграции expand/contract по каждой схеме).
- По умолчанию: мигрируем справочники (контрагенты, программы → статьи) и действующие договоры; закрытые документы остаются как есть, только на чтение.
- Блокер: да.
Ответ: Все имеющиеся справочники которые уже заполнены в проде, необходимо мигрировать, сначала дамп в копию и после поверх с адаптацией

Q-A06. Навигация. Разделу быть переименованными «Договорами» (`/contracts`) или новым разделом «Закупки и оплаты» с подменю по §05 ТЗ? «Мои согласования» (L-03) — это общий инбокс signoff (`/signoff`) или отдельная очередь БЗО?
- Зачем: фронтовый каркас и ссылки в уведомлениях.
- По умолчанию: раздел «Закупки и оплаты» на базе `ContractsShell`; L-03 — фильтр общего инбокса signoff по типам БЗО.
- Блокер: нет.
Ответ: Новый раздел, т.к. много чего сейчас сильно разрозненно и необходимо перенести в одно место для удобства

Q-A07. Ветки и интеграция. Работаем вдвоём в `new-module-BPP-sanzhar` или у Rake887 своя ветка? Куда и как часто мерджим (`pre-production` → `main`)?
- Зачем: конфликты миграций contracts при параллельной работе.
- По умолчанию: у каждого своя ветка от общей; интеграция в общую ветку модуля не реже раза в два дня; миграции создаются только после ребейза на свежую общую ветку.
- Блокер: нет.
Ответ: своя ветка new-module-BPP-ruslan

Q-A08. Сроки и пилот. Есть ли дата для MVP (все Critical из §30)? Какая компания группы — пилот?
- Зачем: порядок этапов и объём MVP.
- По умолчанию: MVP = этапы 0–3 анализа (без сверки и альтернатив); пилот — одна компания.
- Блокер: нет.
Ответ: Сроков нет, главное качество и уровень продакшена. Пилот - Управляющая компания в которой весь Административная часть компания с директорами отделов

Q-A09. Канал к заказчику. Кто собирает ответы на группу B, и кто подтверждает трактовки требований [Л]?
- По умолчанию: Санжар собирает ответы одним документом; до ответа идём по «По умолчанию» этого файла.
- Блокер: нет.
Ответ: Заказчик - моя компания в которой работаю. Вопросы принимаются по факту и на них отвечают по факту появления вопросов. Источник истины ответов: Санжар + Руслан + Алгазы (тимлид+фин.дир (отвечает на логику финансовой стороны модулей))

Q-A10. Документация. Кто обновляет `STRUCTURE.md` §3.4–3.6, `CLAUDE.md`, `API.md`, `docs/apps/contracts|signoff` по ходу работ? Часть уже устарела — см. §9 анализа.
- По умолчанию: каждый — по своим подсистемам в той же задаче.
- Блокер: нет.
Ответ: STRUCTURE.md - Claude по факту нахождения новых структурных особенностей и/или изменений, а CLAUDE.md - также Claude по факту нахождения новых особенностей проекта которые всегда необходимо иметь в виду и держать в памяти чтобы понимать особенности и суть проекта

 B. К заказчику

 B0. Вопросы самого ТЗ (§31) — что по ним уже предрешает платформа

| № | Вопрос ТЗ (коротко) | По умолчанию ТЗ | Что говорит платформа и код |

| В-01 | Одна организация или несколько юрлиц | Одна | Предрешено: платформа мультикомпанейская, схема Postgres на компанию. Бюджеты, документы, нумерация и выписки — в схеме своей компании. Поле `organization_id` не нужно. Уточнить нужна ли сводка по группе ([Q-B35](5)) |
Ответ: ответ по Предрешению, до этого происходил структурный рефакторинг проекта, основой которого стал новая структура компания для сепарации отдельных направлений
| В-02 | Статьи за ролью или за людьми | За ролью | Совместимо с `apps.access` (узел на группу статей). Личные исключения возможны через `RoleAssignment` |
Ответ: За Ролью, так как структура с ABAC перешла на RBAC
| В-03 | СН — все проекты, ПМ — свои | СН все, ПМ свои | Участников проекта в платформе нет — новое ([Q-B17](7), [Q-C16](6)) |
Ответ: владение корректные к этим ролям
| В-04 | Бюджет на весь срок или по периодам | Весь срок | ⚠️ Код и данные — по годам (и по странам): [Q-B01](1) |
Ответ: скорее всего на весь срок
| В-05 | Лимиты с НДС или без | С НДС | Суммы договоров в коде уже «с НДС» (суммы без НДС и НДС считаются отдельно). Подтвердить для порога МРП ([Q-B23](3)) |
Ответ: Лимиты с НДС, Лимиты идут на общие суммы договоров и заявок
| В-06 | Иностранная валюта | Только KZT | В коде `currency` на бюджете, договоре и счёте уже есть, валюта входит в ключ бюджета. Курсов нет. Подтвердить «только KZT» и что делать с существующими не-KZT бюджетами, если они есть |
Ответ: Рассмотреть гибрид, использовать API курса на каждый день, но если у перевода или транзакции есть курс который был фактический на день транзакции к банку, и можно будет отредактировать
| В-07 | Проверка лимита при заявке, резерв | При подаче | ⚠️ Сейчас заявка не резервирует бюджет вовсе: [Q-B06](6) |
Ответ: При подаче, если есть возможность появляения эффекта “Гонки”, необходимо устранить это
| В-08 | Параллельно или последовательно | Решено: последовательно | signoff это умеет (этапы по `order`) |
Ответ: Последовательно
| В-09 | Маршрут от суммы и типа | Единый | signoff умеет ветвление по фактам; можно не использовать |
Ответ: у разных документов свое ветвление, менять которое может только администратор и финансовый директор
| В-10 | Частичная оплата, транши | Только полная, частичные отметки БУХ допустимы | В коде есть отдельная «Предоплата» (одна на договор) и оплаты по договору: [Q-B09](9), [Q-B24](4) |
Ответ: Разные вида, как и Предоплата, как Частичная оплата и транши, более детально уточнить у Алгазы
| В-11 | АВР/накладная до или после оплаты | Только до | ⚠️ В коде АВР и накладная — документы, запускающие оплату (особенно по открытым договорам): [Q-B10](0) |
Ответ: “Закрывающие документы” (АВР/Накладная и Счет-фактура), принимаются только после оплаты
| В-12 | Кто создаёт контрагентов | ФД, БУХ, СН, ПМ; блокирует ФД | ⚠️ В коде контрагент согласуется через signoff: [Q-B14](4) |
Ответ: Все вышеперечисленные лица
| В-13 | Банки и форматы выписок | 1С txt + Excel | Нужны примеры файлов: [Q-B26](6) |
Ответ: файлы с примерами будут приложены в отдельной папке в docs, так как будут возможности оплаты через разные банки
| В-14 | Дробление счетов | Предупреждать ФД | Нет в коде; реализуется как значок в реестре |
Ответ: Добавить данную функцию чтобы ФД мог видеть данный аспект
| В-15 | Изменения договора (допсоглашение) | Новая версия с полным согласованием | Нет в коде; signoff даёт новый круг согласования |
Ответ: нет необходимости делать новый полный круг согласования, лучше чтобы можно было создать как основной договор в том же месте, но указать что это доп.соглашение и чтобы оно ссылалось на основной договор (имело реляцию)
| В-16 | Замещения и срок решения | Замещение через АДМ, 2 раб. дня | ⚠️ В платформе замещение — матрица должностей по приказу (`hr.Substitution`), отсутствий нет; сроков в signoff нет: [Q-B19](9), [Q-B21](1) |
Ответ: Замещения по роли, если роль пуста, либо дожидаются нового на эту роль, либо указывается временный исполнитель данной должности. Сроки отсутствуют
| В-17 | Интеграция с 1С | Нет | Подтвердить: [Q-B34](4) |
Ответ: Планируется в будущем, подготовить заготовки под Интеграцию (1С наш опубликован и имеет связь через OData
| В-18 | Размещение и вход | Сервер в РК, логин/пароль | Предрешено: платформа уже развёрнута, вход по её учётным записям (JWT) |
Ответ: Верно, уже развернуто
| В-19 | Кто выбирает альтернативу по договору | Решено: ФД, ТД, ОД, ГД | Требует доработки signoff (голос за вариант): [Q-C12](2) |
Ответ: Финансовый директор и Генеральный директор
| В-20 | KPI — штуки или экономия | Оба; удорожание отдельно | Нет возражений |
Ответ: Экономия
| В-21 | Когда KPI засчитан | Договор «Действует» или счёт «Оплачено» | Уточнить для счёта: по отметке БУХ или по выписке ([Q-B29](9)) |
Ответ: Счет “Оплачен”
| В-22 | Альтернатива на часть позиций, ожидание | Только на весь документ, 0 часов | Нет возражений |
Ответ: Альтернативна на часть позиций и указывается по каким позициям есть Альтернатива
| В-23 | Кто оформляет документ по альтернативе | СН, предложивший её | Нет возражений; права на документ — через права автора |
Ответ: Автор заявки (СН и/или ПМ)

 B1. Бюджет, проекты, статьи

Q-B01. Единица бюджета. ТЗ: один бюджет на проект на весь срок, изменения — версиями (BR-001, `UNIQUE budget(project_id)`, В-04). Код и данные заказчика (книга CashFlow): бюджет = «администратор (проект + страна) × год × валюта», по одному на год (`contracts/models.py:395`). Что верно для заказчика? Если бюджет на весь срок — как перенести годовые бюджеты: суммировать годы в один лимит или годы станут версиями?
- Зачем: модель данных бюджета, версионирование, CALC-001…003, импорт.
- По умолчанию: один действующий бюджет на проект (как в ТЗ); год — атрибут периода действия; существующие годовые бюджеты одного проекта сводятся в одну версию.
- Блокер: да.
Ответ: суммировать годы в один лимит, бюджет по проекту на весь срок проекта

Q-B02. Версии бюджета. Нужна полноценная версионность со снимками (вкладка «Версии», «Отменить корректировку», печать старой версии) или достаточно журнала правок лимитов с комментарием?

- Зачем: новая сущность `budget_version` и отдельный режим формы — заметный объём.
- По умолчанию: полноценные версии, как в ТЗ (REQ-002 — High), но во втором этапе; в MVP — журнал правок.
- Блокер: нет.
Ответ: Нужна полноценная версионность со снимками  вкладка «Версии», «Отменить корректировку», печать старой версии

Q-B03. Коды статей. ТЗ: код статьи уникален в справочнике (`budget_article.code UNIQUE`). В данных заказчика коды программ свои у каждого проекта: 111 — это «Материально-техническое оснащение» у одного проекта и «Мобилизация/демобилизация» у другого (`contracts/models.py:229-243`). Справочник статей единый для компании (тогда коды придётся перенумеровать) или статьи привязаны к проекту?
- Зачем: структура справочника, импорт, группы статей.
- По умолчанию: единый справочник статей компании с уникальным кодом; «коды проекта» остаются подписью строки бюджета.
- Блокер: да.
Ответ: Единый справочник на всю компанию

Q-B04. Группы статей. Только «Снабжение» и «Проектное управление» или список расширяемый? Может ли статья принадлежать обеим группам? Кто размечает существующие программы по группам?
- Зачем: BR-010 (Critical), миграция справочника.
- По умолчанию: две группы, справочник расширяемый; статья — ровно в одной группе; разметку делает ФД до запуска.
- Блокер: да, для этапа 1.
Ответ: Сделать в виде справочника на вс. компанию

Q-B05. Хранение «Задействовано». ТЗ (CALC-002): материализованное поле с пересчётом в транзакции и ночной сверкой. Платформа принципиально считает остаток на лету: хранимый остаток расходится при правках, удалениях и сбоях. Результат для пользователя одинаков. Согласны на вычисление с ночной сверкой-алертом?
- По умолчанию: да.
- Блокер: нет.
Ответ: Да

Q-B06. Что задействует бюджет. ТЗ (CALC-002, BR-011/012): заявка резервирует сумму с момента отправки; для открытой позиции задействовано = максимум из плановой суммы, договоров по позиции и счетов по позиции. Сейчас (`budget_calc.py:78-99`): договоры с «На согласовании», счета без договора — только после согласования, подотчётные заявки; заявки на закуп не резервируют ничего. Подтвердите формулу ТЗ. Как в ней учитываются:
- (а) подотчётные средства;
- (б) открытые договоры без суммы (сейчас — фактическими оплатами);
- (в) счёт с момента «Отправить ФД» (ТЗ) против момента согласования (код)?
- Зачем: главная формула модуля; от неё зависят все остатки и BR-011, BR-034, BR-043, BR-093.
- По умолчанию: формула ТЗ; подотчёт и открытые договоры — как сейчас, отдельными слагаемыми; счёт — с отправки ФД.
- Блокер: да.
Ответ: По умолчанию

Q-B07. Подотчётные средства и авансовые отчёты. Их нет в ТЗ, но в коде это рабочий процесс с резервом бюджета. Они остаются частью БЗО (своя вкладка, участвуют в «задействовано»), выносятся или выводятся из оборота?
- По умолчанию: остаются как есть, участвуют в «задействовано» отдельным слагаемым.
- Блокер: нет.
Ответ: Решение “По умолчанию”

Q-B08. Страна в бюджете. В коде держатель бюджета — «проект + страна», у одного проекта бывают бюджеты в разных странах. В ТЗ страна — только атрибут контрагента для НДС. Нужно ли сохранить страну как измерение бюджета?
- По умолчанию: нет; при миграции бюджеты проекта по странам сливаются, страна уходит в комментарий строки. Нужно подтверждение.
- Блокер: да, если Q-B01 = «бюджет на проект».
Ответ: Сохранить надо, у одного проекта нету бюджета в других странах, бюджет проекта на страну проекта.

Q-B16. Проекты. «Проекты» БЗО — те же, что на доске задач платформы (объекты, графики, задачи), или отдельный список? Кто их заводит — АДМ и ФД, как в ТЗ?
- По умолчанию: те же проекты платформы, дополненные кодом, заказчиком и руководителем; заводят АДМ и ФД.
- Блокер: да.
Ответ: Заводят проект кто то из администрации (ГД, ФД, ТД, ОД) и также администраторы сайта

Q-B17. Участники проекта. Кто ведёт список участников проекта (для «ПМ видит только свои проекты»): АДМ, ФД или руководитель проекта? Видит ли ПМ проект, в котором он руководитель, но не участник?
- По умолчанию: ведут АДМ, ФД и руководитель проекта; руководитель — участник автоматически.
- Блокер: нет.
Ответ: ПМ и HR (либо проекта, либо управляющей компании)

 B2. Роли, маршруты, согласование

Q-B15. Кто есть кто. Кто конкретно ФД, ТД, ОД, ГД, БУХ, СН и ПМ в каждой компании группы? Совпадают ли роли с HR-должностями (маршрут в платформе адресуется должности)? Бывает ли один человек в ролях нескольких компаний (например, ФД холдинга)? Есть ли компании без ТД или ОД — кто согласует там?
- Зачем: роли в `apps.access`, маршруты signoff, демо-данные.
- По умолчанию: роль = HR-должность с одноимённой системной ролью прав; при отсутствии должности в компании маршрут не запускается до её назначения.
- Блокер: да.
Ответ: В управляющей/руководящей компании находятся директора холдинга, ФД, ТД, ОД, ГД, ГЛ.БУХ относятся к ней. СН также может относится к управляющей компании, но может быть еще и отдельный по проекту. ПМ является сотрудником который является главой проекта и подчиняется директорам управляющей компании

Q-B18. Маршрут заявки для всех сумм. ТД → ОД — для всех заявок, включая мелкие? Правило 8 ТЗ говорит «единый маршрут»; подтвердите.
- По умолчанию: да, единый.
- Блокер: нет.
Ответ: Да

Q-B19. Замещение. ТЗ: АДМ назначает заместителя пользователю на период (отпуск, больничный). В платформе — матрица замещения должностей по приказу (`hr.Substitution`), без учёта отсутствий: «кто сегодня отсутствует» система не знает. Что нужно заказчику?
- (а) Учёт отсутствий (кто, с какой по какую дату) плюс автоматическое переназначение по матрице.
- (б) Ручное назначение «заместитель пользователя на период» без связи с матрицей HR.
- По умолчанию: (а) в упрощённом виде — запись отсутствия с датами (вносит АДМ или кадровик), замещающий берётся из матрицы `hr.Substitution`.
- Блокер: нет (правило помечено [У]).
Ответ: вариант Б выглядит лучше на данный этап

Q-B20. Самосогласование (BR-061). Если автор документа сам стоит на этапе (ГД автор договора, ТД автор заявки), этап переходит «заместителю [У] или ГД». Кому именно, если заместителя нет? А если автор — ГД, а этап — ГД?
- По умолчанию: заместителю по матрице замещения; если его нет — ГД; если автор — ГД, этап пропускается с записью «самосогласование запрещено, этап пропущен» и уведомлением ФД.
- Блокер: нет.
Ответ: вариант “По умолчанию”

Q-B21. Срок решения и эскалация. Нужны ли в первом релизе срок 2 рабочих дня, напоминание и копия ГД? Рабочие дни — по производственному календарю РК (в платформе он есть)?
- По умолчанию: да, во втором этапе; календарь — производственный календарь компании.
- Блокер: нет.
Ответ: по сроку пока говорить рано, лучше использовать ежедневные напоминания если у пользователя имеются подтверждения которые ожидают его решения и отправляются одним Сообщением и напоминанием с списком заявок или подтверждений которые его ожидают в виде текст-ссылки

Q-B22. «Нет исполнителя». ТЗ: если на роль не назначен активный пользователь, документ ждёт на этапе, АДМ и ГД получают уведомление. Сейчас платформа не даёт отправить документ, если на каком-либо этапе нет исполнителя. Какое поведение нужно?
- По умолчанию: как в ТЗ — документ ждёт, АДМ и ГД уведомлены.
- Блокер: нет.
Ответ: По умолчанию

 B3. Договор и контрагенты

Q-B11. Открытый договор. Занимает ли открытый договор бюджет? Сейчас не занимает: расходом считаются фактические оплаты по нему. ТЗ этого не уточняет. Как для него считаются BR-034 и остаток CALC-009 («не рассчитывается»)?
- По умолчанию: как сейчас: бюджет занимают счета по открытому договору.
- Блокер: нет.
Ответ: Открытый договор не занимает бюджета, бюджет занимают платежи и заявки по этому открытому договору

Q-B12. Статусы договора. В коде есть «Согласован» и «Подписан»; в ТЗ после согласования сразу «Действует». Нужен ли шаг «Подписан» (скан подписанного договора после согласования)? Чем «Исполнен» отличается от закрытия всех счетов?
- По умолчанию: «Действует» = согласован; подписанный скан — новая версия файла, без отдельного статуса.
- Блокер: нет.
Ответ: По умолчанию

Q-B13. НДС. Ставка берётся из справочника по стране контрагента на дату договора (BR-031). Сейчас ставка вводится вручную, по умолчанию 12%, а в ТЗ для РК с 01.01.2026 — 16%. Что делать с договорами 2026 года, заведёнными с 12%? CALC-008: сумма договора всегда включает НДС?
- По умолчанию: существующие не трогаем; новые — строго по справочнику; сумма включает НДС.
- Блокер: нет.
Ответ: Вводить вручную при создании договора или другого подтверждения где запрашивается

Q-B14. Контрагенты.
- (а) Сейчас новый контрагент проходит согласование (signoff), в ТЗ — нет, только блокировка ФД. Оставить согласование?
- (б) Статусы в коде «Активен / Неактивен / Заблокирован», в ТЗ «Активен / Заблокирован / Архив».
- (в) БИН сейчас уникален глобально, в ТЗ — пара (страна, рег. номер).
- (г) Нужны ли тип (ЮЛ/ИП/ФЛ/нерезидент), свидетельство НДС, банковские счета с IBAN?
- (д) Нужна ли проверка по госреестру [У]?
- По умолчанию: (а) согласование оставить, если на проде заведён маршрут, иначе — не требовать; (б) «Неактивен» → «Архив»; (в) пара (страна, номер); (г) да; (д) нет.
- Блокер: нет.
Ответ: (а) Убрать согласование, добавить тег Контрагенту (Проверенный), выставляется тем контрагентам, с которыми было удачно составлено несколько договоров, или счетов (уточняется у Автора заявки, когда он отправил документ на согласование. Если контрагент уже подтвержден, про него не выходит это окно. (б) Да, измени согласно ТЗ. (в) Да, будут контрагенты с разных стран, у всех свои методы идентификации. (г) Да. (д) В будущем можно будет сделать, сейчас нет надобности

Q-B38. Контрагенты-физлица и нерезиденты. Нужны ли в первом релизе контрагенты-ФЛ, ИП и нерезиденты со свободным рег. номером до 30 символов?
- По умолчанию: да, поле `kind` с первого релиза.
- Блокер: нет.
Ответ: Да

 B4. Счета, оплаты, закрывающие документы

Q-B09. Единый счёт. ТЗ: один «Счёт на оплату» с основанием («без договора» / «по договору») и сквозным номером `СЧ-ГГГГ-NNNNNN`, по которому идёт сверка с банком. В коде три документа: «Счёт на оплату без договора», «Оплата по договору» и «Предоплата по договору» (одна на договор), у каждого своё согласование. Сводим в единый счёт (предоплата = счёт по договору с признаком «аванс»)?
- Зачем: модель, номер для сверки, реестр ФД и БУХ, миграция данных.
- По умолчанию: да, единый счёт; существующие оплаты и предоплаты мигрируют в счета по договору.
- Блокер: да.
Ответ: По умолчанию

Q-B10. АВР и накладная. ТЗ: это файлы, которые БУХ запрашивает у автора счёта до оплаты, без согласования. Код: самостоятельные документы по договору со своим согласованием, по которым бухгалтер проводит оплату; для открытого договора это основной способ расходовать бюджет. Нужны ли акты как документы с суммой (например, для учёта выполнения по договору или для ЭСФ), или достаточно файлов на счёте?
- По умолчанию: закрывающие документы — файлы на счёте, как в ТЗ; оплата открытого договора — счётом по договору; существующие акты и накладные мигрируют в счета по договору с приложенным файлом.
- Блокер: да.
Ответ: Закрывающие документы получаются после оплаты, так как расчет в основном идет за счет “Счетов на оплату”.

Q-B23. Порог 1000 МРП. Сравнивается сумма счёта с НДС? МРП — на дату счёта контрагента? Если счёт в валюте — по какому курсу (при «только KZT» вопрос снимается)?
- По умолчанию: сумма с НДС, МРП на дату счёта контрагента.
- Блокер: нет.
Ответ: По умолчанию

Q-B24. Частичная оплата. Нужен ли статус «Оплачено частично» [У] и повторные отметки оплаты по одному счёту (транши)?
- По умолчанию: да, несколько отметок; статус «Оплачено» — когда Σ отметок = сумме счёта, до того — «Оплачено частично».
- Блокер: нет.
Ответ: По умолчанию

Q-B39. Решение ФД и плановая дата. «Оплатить» ставит плановую дату оплаты и может быть массовым. Нужен ли отдельный этап согласования счёта кроме ФД (например, руководитель проекта)? ТЗ говорит «только ФД».
- По умолчанию: только ФД.
- Блокер: нет.
Ответ: Уточнить у Алгазы

 B5. Выписки, сверка, дашборд

Q-B26. Банки и выписки. Какие банки и счета у каждой компании, в каком формате выгружается выписка (1CClientBankExchange .txt, xlsx конкретного банка)? Нужны реальные обезличенные примеры файлов. Бывают ли платежи в валюте? Возвраты и поступления не сверяем?
- Зачем: без примеров парсеры и шаблоны маппинга колонок не написать.
- По умолчанию: 1С txt + xlsx/csv с маппингом колонок в справочнике; только списания в KZT.
- Блокер: да, для этапа 4.
Ответ: Примеры выписок приложу в отдельную папку в docs в папке проекта

Q-B27. «Близкая сумма». Какой допуск для предложения кандидатов при ручном сопоставлении?
- По умолчанию: точное совпадение с остатком — первым, затем по возрастанию разницы в пределах ±5%.
- Блокер: нет.
Ответ: По умолчанию, но сделать в пределах ±10%

Q-B40. Несколько номеров в одном платеже (§11.3 п.6 [У]). Распределять сумму по неоплаченным остаткам в порядке номеров — подтверждаете?
- По умолчанию: да; при несовпадении — «Требуют проверки».
- Блокер: нет.
Ответ: По умолчанию

 B6. Альтернативы и KPI

Q-B28. Альтернативы. Подтвердите значения по умолчанию §12: до 3 АП на документ, одно от СН; только на весь состав; сравнение с НДС; допуск 5% между АП и новым документом; ожидание 0 часов. Кто вправе подавать АП — только СН или и ПМ?
- По умолчанию: как в ТЗ; подаёт только СН.
- Блокер: нет.
Ответ: Количество АП увеличивает сам СН, также ПМ может предложить до 3 АП, остальное как в ТЗ

Q-B29. KPI по счёту. KPI «Подтверждён» по новому счёту — по отметке БУХ «Оплачено» или по сверке с выпиской?
- По умолчанию: по отметке БУХ; сверка — дополнительная отметка в отчёте.
- Блокер: нет.
Ответ: По умолчанию

 B7. Прочее

Q-B25. Антивирус. Нужна ли антивирусная проверка файлов при загрузке [Л]? Это отдельный сервис (например, ClamAV) в инфраструктуре.
- По умолчанию: нет в MVP.
- Блокер: нет.
Ответ: Оставить задел на потом, но пока подготовить под ClamAV

Q-B30. Каналы уведомлений. В системе + e-mail — обязательны оба? Telegram [У] нужен? Сводка ФД в 09:00 по Алматы — только по счетам на решении?
- По умолчанию: колокольчик + e-mail; Telegram — нет; сводка — счета «На рассмотрении ФД».
- Блокер: нет.
Ответ: Колокольчик + email + telegram (управляется у пользователя в настройках, минимально должен быть хотя бы 1 метод), сводка - также отправляется из списка документов, которые ожидают подтверждения со стороны этого пользователя 

Q-B31. Печать заявки (PDF). Нужен ли бланк компании (логотип, реквизиты)? Лист согласования — таблица этапов?
- По умолчанию: простой шаблон без бланка, лист согласования — таблицей этапов.
- Блокер: нет.
Ответ: Бланк нужен (колонтитулы будут также потом в файле в docs папке, контент будет печататься согласно форме, шаблоны данных форм после будет утверждаться

Q-B32. Аудит. Срок хранения 5 лет [У] — подтвердите. Нужен ли журнал скачиваний файлов (ТЗ §25.2 требует)?
- По умолчанию: 5 лет; скачивания журналируются.
- Блокер: нет.
Ответ: По умолчанию

Q-B33. Размещение и вход. Модуль работает на текущей платформе (своя авторизация, свой сервер). НФТ ТЗ «сессия 8 ч, неактивность 60 мин, блокировка после 5 неудачных входов» — применять ко всей платформе?
- По умолчанию: да, для всей платформы; реализация — платформенная задача.
- Блокер: нет.
Ответ: Нынешнее состояние платформу соответствует, можно добавить дополнительные уровни защиты на данный модуль


Q-B34. 1С. Интеграция с 1С (В-17) точно не нужна в первом релизе, включая выгрузку контрагентов?
- По умолчанию: не нужна.
- Блокер: нет.
Ответ: пока что да, готовить к скорому подключения после успешного запуска модуля

Q-B35. Сводка по группе. Нужна ли ФД или ГД холдинга сводка бюджетов и оплат по всем компаниям группы (платформа умеет сводные представления `holding`)?
- По умолчанию: нет в MVP; модели БЗО сразу объявляются в `holding.py`, чтобы сводку можно было добавить без миграций.
- Блокер: нет.
Ответ: По умолчанию

Q-B36. История. Переносить ли в новую модель исторические (закрытые) бюджеты, договоры и счета, или достаточно открытых на дату старта? Связано с [Q-A05](5).
- По умолчанию: только открытые; закрытые — на чтение в старом виде.
- Блокер: да, вместе с Q-A05.
Ответ: По умолчанию

Q-B37. Казахский язык [У]. Нужен в первом релизе? В платформе i18n уже есть.
- По умолчанию: нет, только русский.
- Блокер: нет.
Ответ: Переводчик уже есть, но пока что не делать переводы новых элементов, их перевод будет добавляться позже

 C. Архитектурные (решаем вдвоём)

Q-C01. Дом модуля. Развиваем `apps.contracts` как единый домен БЗО или создаём новую аппку и переносим туда модели contracts?
- Зачем: межаппочный FK запрещён, а цепочка «строка бюджета → позиция заявки → позиция договора / строка счёта → строка выписки» должна держаться на FK и считаться в одной транзакции (BR-011, CALC-002/005, §11.3 п.8).
- По умолчанию: развиваем `contracts`.
- Блокер: да.
Ответ: Новая аппка, чище и легче будущая поддержка, изначально contracts считалась только часть всего этого модуля, поэтому делаем новую

Q-C02. Разнесение contracts на пакеты. Перед параллельной работой разносим `models.py` (1270 строк), `views.py` (1539), `schemas.py` на пакеты по поддоменам (budget, requests, plan, agreements, invoices, bank, alternatives, references). Как организуем миграции: одна цепочка, кто создаёт, как разрешаем параллельные номера (merge-миграции)?
- По умолчанию: разносим на этапе 0 одним коммитом без изменения поведения; миграции — только после ребейза на общую ветку; конфликт номеров — merge-миграцией.
- Блокер: да, для параллельной работы.
Ответ: Да, в любом случае новая аппка будет

Q-C03. Модуль в реестре. Оставляем имя сервиса `contracts` в `KNOWN_SERVICES`/`CompanyModule`? Нужны ли отдельные рубильники подмодулей (сверка, альтернативы) или они включаются вместе?
- По умолчанию: один модуль `contracts`, подмодули — без отдельных рубильников.
- Блокер: нет.
Ответ: Новая аппка и подмодули с рубильниками

Q-C04. «Проект» ТЗ. Три кандидата:
- (а) `tasks.Project` — дополнить кодом, заказчиком, руководителем и участниками;
- (б) `contracts.Administrator` (проект × страна) — дополнить там же;
- (в) новая сущность в contracts.

Направление зависимостей: сейчас contracts ссылается на tasks (`Administrator.project_id`). Заказчик проекта = контрагент из contracts, то есть tasks начнёт ссылаться на contracts — появится двусторонняя зависимость через interface.
- По умолчанию: (а); заказчик хранится голым `customer_counterparty_id`, подпись берётся через `contracts.interface`; `Administrator` становится звеном «проект (+ страна)» или сливается с проектом по ответу Q-B08.
- Блокер: да.

Ответ: (а) по умолчанию. (б) по умолчанию. (в) да, новая сущность, так как задействуется много где, а заказчики в (а) потому что их много на разных проектах, и держать их отдельно пока нецелесообразно, но можно в будущем

Q-C05. Модель заявки. Типизированная `PurchaseRequest` + `PurchaseRequestItem` в contracts, зарегистрированная в signoff как `contracts.purchase_request` с маршрутом ТД → ОД? Или шаблон конструктора с доработкой виджетов?
- Зачем: резерв под блокировкой, FK позиций, статусы позиций, План закупок.
- По умолчанию: типизированная модель.
- Блокер: да.
Ответ: По умолчанию

Q-C06. Позиция договора. `AgreementItem.request_item_key` — это текстовый ключ строки JSON заявки. Переводим на FK к `PurchaseRequestItem`? Что делаем со старыми позициями (заявки-шаблоны)?
- По умолчанию: новый FK рядом со старым ключом (expand), старые позиции остаются по ключу.
- Блокер: нет.
Ответ: По умолчанию

Q-C07. Единый счёт в коде. Если заказчик подтвердит Q-B09, выбираем способ:
- (а) одна модель `Invoice` с `basis`, `contract_id`, номером, строками; `ContractPayment` и `AdvancePayment` мигрируют в неё;
- (б) три модели сохраняются, поверх — общий реестр с общим номером.
- По умолчанию: (а).
- Блокер: да.
Ответ: Уточнить у Алгазы

Q-C08. Статусы. Сохраняем две оси — `status` (жизненный цикл) и `approval_state` (signoff)? Составить таблицу соответствия статусов ТЗ этим двум осям для заявки, договора и счёта. «На доработке» и «Отклонён» ТЗ — это `approval_state` или `status`?
- По умолчанию: две оси; статус, который видит пользователь, вычисляется из пары.
- Блокер: нет.
Ответ: По умолчанию

Q-C09. Доработки signoff — для всех или по флагу. Запрет самосогласования, обязательный комментарий ≥10 символов при отказе и возврате, замещение, «нет исполнителя», сроки — это изменения движка. Они затронут согласование заявок конструктора и десяти кадровых предметов `hr.`, чей маршрут и набор фактов — контракт со вторым разработчиком HR (roadmap §6.4). Включаем глобально или настройкой этапа/маршрута?
- По умолчанию: настройкой на уровне маршрута (флаги), по умолчанию выключено; для маршрутов БЗО — включено.
- Блокер: да.
Ответ: По умолчанию

Q-C10. Разрешение исполнителей. Сейчас исполнители всех этапов определяются на запуске, и запуск отказывает, если где-то нет исполнителя. ТЗ: исполнитель определяется при создании этапа (с учётом замещения на тот день), при отсутствии — статус «Нет исполнителя». Переходим на ленивое разрешение при активации этапа?
- По умолчанию: да, для маршрутов с новым флагом (см. Q-C09).
- Блокер: нет.
Ответ: По умолчанию

Q-C11. Сроки и эскалация. Где считать рабочие дни? Производственный календарь живёт в `tasks.ProductionDay` — signoff получит зависимость от tasks через interface. Периодическая задача — Celery beat в signoff.
- По умолчанию: календарь через `tasks.interface`; при выключенном tasks — календарные дни с записью в fallback.
- Блокер: нет.
Ответ: Вытащить календарь отдельной аппкой и все уже будут подтягивать из него напрямую и можно будет добавлять для него разные методы обращений под всех

Q-C12. Голосование за альтернативу. Как выразить «согласовать исходный / согласовать АП-…» и решающий голос ГД?
- (а) Новый тип решения в signoff — общая возможность «выбор варианта из объявленных объектом».
- (б) Решение остаётся `approve`, а выбранный вариант — доменная запись, приложенная к задаче до решения (по образцу `attachments` / `requirement_key`).

Плюс предсогласованные этапы нового документа («Согласовано при выборе альтернативы»).
- По умолчанию: (б) для голоса; предсогласованные этапы — новая возможность движка.
- Блокер: нет (этап 5).
Ответ: По умолчанию, но также гибридно адаптировать и (а)

Q-C13. Решение ФД по счёту и массовые решения. Решение ФД — одноэтапный маршрут signoff (`approve` = «Оплатить» с плановой датой как полем согласующего, `reject` = «Не к оплате», `rework` = «Вернуть»)? Действия БУХ — доменные операции счёта, не этапы? Нужна ли в signoff ручка массовых решений (для «Оплатить отмеченные» и «Согласовать отмеченные»)?
- По умолчанию: да, да, да; массовое решение — по одному процессу в своей транзакции, ответ — список успехов и отказов с причинами.
- Блокер: нет.
Ответ: По умолчанию

Q-C14. Роли ТЗ в `apps.access`. Заводим 8 системных ролей (миграцией, как `hr-junior…hr-lead`) с узлами `contracts.`? Права выдаются должностям (`PositionRole`), согласующие в маршрутах — те же должности. «Роль инициатора» при совмещении СН + ПМ — из того, какие группы статей пользователю доступны.
- По умолчанию: да.
- Блокер: да.
Ответ: По умолчанию, но роли будут утверждаться на момент их создания

Q-C15. Группа статей в модели прав. Как связать группу с правами:
- (а) узел реестра на каждую группу (`contracts.articles.supply`, `contracts.articles.pm`) — просто, но новая группа требует узла;
- (б) у группы поле «ключ узла» — расширяемо, но узел всё равно объявляется в реестре.
- По умолчанию: (б) с двумя узлами на старте.
- Блокер: нет.
Ответ: По умолчанию

Q-C16. «Свои» проекты и документы. Фильтр «ПМ — только проекты, где он участник», «СН/ПМ — только свои заявки и счета» делаем доменной проверкой в сервисах contracts (как `approvals.permissions`), без нового `ScopeKind`?
- По умолчанию: доменной проверкой.
- Блокер: нет.
Ответ: По умолчанию

Q-C17. Где живут справочники. Валюты, курсы, МРП, ставки НДС, страны, ед. изм. — общие для группы по смыслу (законодательство РК). Варианты:
- (а) в схеме `public` (новая общая аппка или `core`) — одна правда, но contracts тенантная и достаёт их только через interface;
- (б) в схеме каждой компании — дубли, но проще FK.

`Country` сейчас лежит в contracts (тенантная).
- По умолчанию: (а) для МРП, ставок НДС, валют, курсов и ед. изм.; `Country` переносится туда же; на документах хранятся снимки значений (ставка, МРП на дату).
- Блокер: да, для этапа 1.
Ответ: Проект сейчас мультитенантный и данный справочники лучше хранить в одной управляющей компании, а Авторы будут подтягивать их за счет доступа к Заявкам, и только в пределах данного модуля

Q-C18. Идемпотентность и версии. Idempotency-Key и оптимистическая блокировка (`version`, 409) — общий механизм в `htqweb/http.py` для всех аппок или только для contracts? Где хранить ключи (Redis с TTL)?
- По умолчанию: общий механизм с включением по флагу на ручке; ключи — в Redis на 24 часа; `version` — только на документах БЗО.
- Блокер: нет.
Ответ: По умолчанию

Q-C19. Уведомления. Сейчас нет единого хранимого центра уведомлений: колокольчик — `tasks.Notification`, signoff шлёт через Socket.IO без хранения. Для БЗО нужен колокольчик + e-mail с гарантией доставки. Варианты:
- (а) outbox-таблица + Celery-воркер;
- (б) `on_commit` + Celery-задача с повтором.

И где хранить уведомления — оставить `tasks.Notification` или вынести в общий домен?
- По умолчанию: (б) и хранение в `tasks.Notification` через interface (новый `target_type`); вынос в общий домен — отдельной задачей позже.
- Блокер: нет.
Ответ: Выносим уведомления в отдельный хранимый центр уведомлений

Q-C20. Неизменяемый аудит. Таблица аудита в contracts по образцу соседних аппок. Запрет UPDATE/DELETE на уровне БД (§25.2): триггером или отдельной ролью БД для миграций? Сейчас приложение и миграции работают под одной учёткой.
- По умолчанию: триггер БД, запрещающий UPDATE/DELETE, плюс запрет в коде; отдельную роль БД не заводим.
- Блокер: нет.
Ответ: По умолчанию

Q-C21. Формат ошибок. Отдаём `{"detail": "<готовый текст>", "code": "E-BUD-01", "fields": [{field, message}]}` — аддитивно к конверту платформы? Фронт читает `detail` как прежде, `code` — для машинной обработки.
- По умолчанию: да.
- Блокер: нет.
Ответ: да

Q-C22. Миграции тенантной аппки. Смена моделей бюджета и счёта — это expand/contract по всем схемам компаний через `migrate_companies`; представления `holding` на время сносятся. Какой план выкатки: сколько окон обслуживания, что делаем с компаниями на разных версиях?
- По умолчанию: каждый этап — пара «expand-миграция + перенос данных командой», contract-миграции — отдельным релизом после проверки на всех схемах.
- Блокер: да, для этапов 1–3.
Ответ: Миграции сразу после пройдут все разом, как модуль будет полностью готов, полностью его мигрируем в продакшен

Q-C23. Первичные ключи. ТЗ требует UUID PK; платформа везде на целых. Остаёмся на целых?
- По умолчанию: да.
- Блокер: нет.
Ответ: Переделываем на согласное ТЗ

Q-C24. Печать PDF. Какая библиотека? WeasyPrint требует системных библиотек в Docker-образе; ReportLab — нет. Влияет на `backend/Dockerfile` (5 процессов собираются из одного образа).
- По умолчанию: HTML-шаблон + WeasyPrint в основном образе, если прирост образа приемлем; иначе ReportLab.
- Блокер: нет.
Ответ: По умолчанию

Q-C25. Экспорт xlsx до 50 000 строк. Синхронно (потоковая запись openpyxl) или фоновой задачей с файлом в media и ссылкой?
- По умолчанию: синхронно до 5 000 строк, больше — фоновой задачей.
- Блокер: нет.
Ответ: Синхронно до 10 000 строк, больше - фоном

Q-C26. Асинхронная загрузка выписки. 5 000 строк ≤ 60 с «асинхронно с прогрессом»: Celery `@company_task` + опрос статуса загрузки фронтом (или SSE, как `/requests/stream`)?
- По умолчанию: Celery + опрос.
- Блокер: нет.
Ответ: По умолчанию

Q-C27. Нумерация. Единый годовой счётчик в схеме компании для `БДЖ-`, `ЗЗ-`, `ДГ-`, `СЧ-`, `ВП-`, `АП-`. Присваиваем ли номера существующим договорам и счетам задним числом? `БДЖ-<код проекта>` — это не счётчик, а код проекта.
- По умолчанию: существующим — присвоить при миграции в порядке создания; `БДЖ-` — из кода проекта.
- Блокер: нет.
Ответ: По умолчанию

Q-C28. Файлы документов. Таблица «файл документа» (владелец, тип, версия, «заменён», кто и когда загрузил) в contracts поверх `media_files`? Типы, форматы, размеры и лимиты «до N файлов» — справочником или константами?
- По умолчанию: таблица в contracts; типы — константами с политиками scope в `media_files`.
- Блокер: нет.
Ответ: По умолчанию

Q-C29. Наблюдаемость. Какие бизнес-метрики БЗО нужны (очередь ФД и БУХ, несопоставленные списания, просрочки этапов)? Каждая требует панели или правила — сторож `test_metrics_are_observed`.
- По умолчанию: 3–4 метрики с панелями на дашборде `htqweb-domains`.
- Блокер: нет.
Ответ: По умолчанию, после еще добавим по запросу

Q-C30. Каркас реестров на фронте. Серверные фильтры и сортировка, наборы колонок и фильтров (где хранить: `localStorage` или сервер), массовые операции, итоговая строка, экспорт — общий компонент на базе `CollectionPage`? Автосохранение черновика формы в `localStorage` каждые 30 с — общий хук?
- По умолчанию: общий компонент и хук; наборы колонок — в `localStorage`.
- Блокер: нет.
Ответ: По умолчанию

Q-C31. НФТ платформы. Блокировка входа после 5 неудач, rate limit 100 запросов/мин, correlation-id в логах — делаем в рамках БЗО или отдельными платформенными задачами?
- По умолчанию: отдельные платформенные задачи исполнителя A, вне критического пути.
- Блокер: нет.
Ответ: К нынешнему можно добавить блокировку после 5 неудач и rate-limit 300 запросов в минуту

Q-C32. Тесты и приёмка. Какие из AC-001…AC-021 и сценариев SC-001…006 автоматизируем e2e (Playwright), а какие — pytest? Для гонок (AC-004, AC-013) нужны тесты с параллельными транзакциями.
- По умолчанию: все AC — pytest; SC-001…006 — e2e на пилотной компании.
- Блокер: нет.
Ответ: По умолчанию


 D. По существующему коду и данным (к авторам `apps.contracts`)

Q-D01. Что на проде. В каких схемах компаний есть данные contracts и сколько их по типам (бюджеты, строки, договоры, счета, оплаты, АВР, накладные, предоплаты, подотчёт)? Какие документы реально используются в работе?
Ответ: Есть Договора и контрагенты, также документы некоторые, в работе не прям чтобы используются, но терять не желательно, либо делаем дамп, либо другой бэкап с последующим аккуратным переносом

Q-D02. Решения, расходящиеся с ТЗ. Были ли договорённости с заказчиком о том, что:
- у счёта без договора нет номера; Ответ: Создается автоматически свой внутренний номер по заявке
- счёт занимает бюджет только после согласования; Ответ: Да
- бюджет ведётся по годам и странам; Ответ: Бюджет ведется на проект и открытый (как например на основную компанию
- расторжение освобождает всю сумму договора? Ответ: Освобождает, но только не освоенную часть бюджета по договору

Q-D03. Маршруты signoff на проде. Какие маршруты заведены для типов `contracts.`, кто в них согласующие (должности)? Заявки какого шаблона на закуп сейчас на согласовании?
Ответ: Маршруты некоторые существуют, но лучше сделать дамп и после их адаптировать

Q-D04. Незавершённые работы. Есть ли незамёрдженные ветки или задачи по contracts (ветка `ruslan` и др.), которые пересекаются с БЗО?
Ответ: есть мелкие, пересекаются, но они минорные и можно будет проанализировать их применимость в модуле

Q-D05. Импорт CashFlow. Он разовый или будет повторяться? Если повторяется — после смены модели его придётся переписать.
Ответ: Перепишем

Q-D06. Обязательность позиций договора. Почему `ITEMS_REQUIRED_FOR_APPROVAL = False` (`agreement_items.py:166`) и что нужно, чтобы включить (ТЗ требует ≥1 позицию)?
Ответ: Да, в договоре должна быть хотя бы 1 позиция, нужен же предмет договора

Q-D07. Ключи `Position.permissions`. У каких должностей на проде выставлены `contracts.contract_payment.record_payment`, `contracts.advance_payment.record_payment`, `contracts.accountable_funds_request.mark_paid`? Их надо перенести в роли `apps.access` без потери доступа бухгалтеров.
Ответ: Пока что роли полностью после рефакторинга не переносились, т.к. рефакторинг все еще не в проде, и можно будет все разом применить к продакшену

Q-D08. Ставка НДС по умолчанию 12%. Сколько договоров 2026 года заведено со ставкой 12% при действующих в РК 16% (по данным ТЗ)? Это ошибка ввода или так и было по документам?
Ответ: Ставка НДС по умолчанию 16%, но может редактироваться в документе